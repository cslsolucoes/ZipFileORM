/* sys/file.h Windows compat stub for LHa SDK static-link build.
 * LHa SDK includes <sys/file.h> for flock() prototype which we don't
 * use in decoder paths. Empty stub is sufficient.
 */
#ifndef _LHA_COMPAT_SYS_FILE_H
#define _LHA_COMPAT_SYS_FILE_H
#endif
